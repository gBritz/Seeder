using System;

namespace Seeder.Conventions
{
    public class CityNameConvention : IConvention
    {
        public Boolean IsSpecifiedBy(StructureCode structure)
        {
            throw new NotImplementedException();
        }
    }
}